package interview_skeletons;

/**
 * It is raining and drops keep falling on a 100 cm line. Every drop has 1 cm diameter. When a drop falls then a function is called.
 * On each fall, the function should return true or false. If the line is not covered completely then the function will return false.
 * When the complete line is covered, the function will start returning true.
 */
public class LineCoveredByDrops {

    public boolean drop(double i){
        return false;
    }
}
